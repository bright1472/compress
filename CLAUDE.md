# 核心检索协议 (CRITICAL PROTOCOL)
本项目已通过 MCP 接入 `qmd-search` 本地向量引擎。
为了保证最高效的上下文利用率，你 **必须** 遵守以下强制规则：

1. **禁止暴力扫描**：绝对禁止使用原生的 `grep`、`find` 或大面积的 `ls -R` 去寻找代码逻辑。
2. **强制使用工具**：当需要查找函数、梳理业务逻辑或定位文件时，**必须优先调用 `qmd-search` 提供的 MCP 工具**。
3. **按需阅读**：不要一次性完整读取超过 500 行的长文件，通过 qmd 返回的精确代码片段（Snippets）来理解上下文。
